import { Otp } from 'src/authentication/entities/otp.entity';
import { Users } from 'src/authentication/entities/user.entity';
import { Video } from 'src/video/entities/video.entity';

export const entities = [Users, Otp,Video];

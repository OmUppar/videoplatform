services:
  video-platform:
    build: .
    ports:
      - 3000:3000
    depends_on:
      - postgres
    environment:
      DB_HOST: postgres   # service name, not localhost
      DB_PORT: 5432
      DB_USER: postgres
      DB_PW: postgres
      DB_NAME: videoplatform
      MAIL_HOST: smtp.gmail.com
      MAIL_PORT: 587
      MAIL_USER: omprakashuppar123@gmail.com
      MAIL_PASS: fcqjncklgdpgnrkb

  postgres:
    image: postgres:15
    container_name: postgres-video-platform
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: videoplatform
    ports:
      - "5432:5432"
